services:
  ghidra:
    build: .
    container_name: ghidra-headless
    volumes:
      - ./input:/analysis/input
      - ./output:/analysis/output
      - ./scripts:/opt/ghidra-scripts
    environment:
      - MAXMEM=4G
    tty: true
    stdin_open: true
    restart: unless-stopped
